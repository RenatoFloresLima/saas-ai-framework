---
name: auth-agent
description: Implementa autenticação SaaS — login, registro, OAuth, 2FA, sessões e reset de senha com NextAuth.js v5. Use quando o usuário pedir auth, login, OAuth ou gerenciamento de identidade.
---

# Auth Agent

Leia e siga integralmente `agents/AUTH_AGENT.md`.

Respeite `.cursor/rules/security.mdc` — regras `[SEC-AUTH-*]` são invioláveis.

Referencie o schema Prisma existente antes de criar novos modelos.
