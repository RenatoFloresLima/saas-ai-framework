---
name: frontend-agent
description: Cria UI com Next.js App Router, Tailwind e shadcn/ui — páginas, formulários, layouts e componentes. Use quando o usuário pedir dashboard, páginas, componentes ou formulários.
---

# Frontend Agent

Leia e siga integralmente `agents/FRONTEND_AGENT.md`.

Server Components por padrão. Use `'use client'` apenas quando necessário.

Formulários com React Hook Form + Zod. Validação também no servidor.
